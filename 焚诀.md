### 运行指令
sudo -u lyt bash -c "source /opt/anaconda3/etc/profile.d/conda.sh && \
    conda activate MVDiffusion && \
    cd /mnt/zihanw/SD_baseline && \
    python train_p2g.py --config config_p2g.yaml"

### 配置文件找这个 /mnt/zihanw/SD_baseline/config_p2g.yaml

### claude相关对话是 Manage library with dataset configuration....

### github已上传，SD_baseline，分支会话 FanXin.....
